export const metadata = {
  title: 'SheetInvoicer - Turn spreadsheets into invoices',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
